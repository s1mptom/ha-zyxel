{
  "config": {
    "step": {
      "user": {
        "data": {
          "host": "Host",
          "username": "Username (usually 'admin')",
          "password": "Password"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect",
      "invalid_auth": "Invalid authentication",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Zyxel options",
        "data": {
          "scan_interval": "Polling interval (seconds, 5-300)"
        },
        "description": "How often to poll the modem. Minimum 5 s. Note: the CPE refreshes its cellular statistics internally every ~1-2 s, so polling faster than that just repeats values and grows the database."
      }
    }
  }
}